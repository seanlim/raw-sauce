const tk = require('../tokens');
const nodeTemplate = require('./nodeTemplate');
const statementParser = require('./statementParser');

// Parses functions

module.exports = (tokens) => {
  console.log(tokens);
  if (tokens.indexOf(tk.OPEN_PAREN) != -1 ){
    
  }
}

function buildTree() {

}